#include "main.h"
/**
 * _strlen - is a function that counts the length of a string
 * @n: is a pointer to the begining of the string
 * Return: Number Of Counted Printed Charecters
 */

int _strlen(const char *n)
{
	int i;

	for (i = 0; n[i] != '\0'; i++)
		return (i);


}
